# 運用ルール（毎回チャット冒頭で読み込む）

読み込んだら最初の発言冒頭に「CHAT_RULES読み込み済み」と明記すること。

## チャット側Claudeの役割
設計判断・検証・調査結果の議論・Claude Code向け指示書の作成を担う。
リポジトリのファイル変更そのものは行わない（詳細は下記「原則」）。

## 原則
On-a-journeyリポジトリのファイル変更は、すべてClaude Codeに依頼する。
チャット側Claudeは直接編集・作成しない（typo修正・1行追記も例外なし）。

## 手順
ファイル変更が必要になった時点で、作業前に必ず以下を確認する：

「Claude Codeへの依頼文を作成してよいですか？」

確認なしに create_file / str_replace 等でリポジトリの実体ファイルを
編集・作成してはならない。

**確認が不要なケース（2026-07-10の教訓）**：
上記の確認は、リポジトリのファイルを変更・作成する依頼（BACKLOG.md/
SYSTEM_MAP.md/CHAT_RULES.md等への追記、ソースコードの修正等）に限定して適用する。
既存ファイルの**読み取りのみ**を行い、新規ファイル（一時的な集計JSON等、
本番ファイルではないもの）の出力にとどまる依頼（データ抽出・妥当性チェック・
分析用スクリプトの実行等）については、確認を挟まずそのまま依頼文を作成してよい。

## 調査中に発見したバグの取り扱い（2026-07-11の教訓）
調査・診断依頼の実行中に、依頼内容と無関係な別のバグを発見した場合、
その場で実装修正・データ再生成を行ってはならない。調査依頼は
「発見・報告」までを範囲とし、修正の実装は別途チャット側Claudeが
依頼文を作成し、Koichiさんの確認を経てから着手する。

これは「不具合修正は根本対応する」という原則（下記「設計判断の確認方法」参照）
と矛盾しない。根本対応するのは「修正すると決まった後」の話であり、
「調査中に無許可でその場修正してよい」ことにはならない。

例外: 調査の続行自体が不可能になる程度の軽微な妨げ（一時ファイルの
片付け等、本番ファイルに影響しないもの）はこの限りではない。

**多段連鎖の実例（2026-07-11、POLICYB-GATE-FIX-1〜TTM-QUARTERS-CHECK-1）**:
1件の依頼に対し「発見・報告→実装依頼→検証で別バグ発見→報告」が
4段階連鎖した（Policy Bゲート修正→floor_applied回帰発見→growth_sanity
未連動発見→CAGR符号バグ発見→TTMデータ完全性バグ発見）。各段で
実装せず報告のみに留めるという本ルールの運用は継続する。連鎖が
深くなること自体は異常ではなく、根本原因を辿った結果であり、
都度の報告・承認プロセスを省略する理由にはならない。

## 検証結果が依頼の前提と乖離した場合の一時停止（2026-07-15の教訓）
「最小修正」等スコープを限定して実装を依頼した場合でも、実データでの
検証中に「依頼時に想定していた挙動（例: 誤った値→明示的に値なしへの
変換）を実現できない」と判明することがある。このような場合、
Claude Codeは想定と異なる結果のままコミットして完了報告するのではなく、
実際の挙動（何がどう変わり、何が変わらなかったかの具体的なbefore/after）
を提示した上で一時停止し、①想定と異なる結果のまま採用する
②実装を復元し別方針を検討する③追加のロジックで対応する、のいずれを
望むかユーザーに確認することが適切な対応である。

（実例: FY52WEEK-BUCKET-MISPLACE-1のduration filter実装。「四半期スタブ
除外により誤った値→値なし」を想定していたが、実データ検証の結果、
一部年度は隣接年度の値へラベル誤りのまま横滑りするだけで値なしに
ならず、DELLでは別種の衝突により真の値自体が消失したままと判明。
Claude Codeは独断でコミットせず一時停止して確認を求め、結果として
実装を復元しBACKLOG記録のみに切り替えた。この対応は適切だったため、
標準パターンとして明文化する）

## str,Enum型導入時の実装・検証パターン（2026-07-18の教訓）
生文字列の代入・比較箇所を`class X(str, Enum)`で型化する際は、以下2点を
標準手順とする（GATE2-PHASE3B-1③-a/③-bで2回連続で発生・解消した教訓）。

1. **`__str__`のoverrideを必須にする**: Python 3.11以降、`str, Enum`を
   継承していてもEnumの`__str__`/`__format__`はデフォルトで
   `ClassName.MEMBER`というクラス名付き表記を返す仕様に変わっている
   （3.10以前は素の文字列を返していた）。f-string補間・`str()`は
   `__str__`をoverride（`def __str__(self): return self.value`）しない
   限り期待通りに動作しない。`==`比較・`json.dumps()`（str継承の
   isinstance高速パスによりoverride不要で正しく動作）はこの問題と無関係
   だが、report.txt等のテキスト生成・ログ出力・print文で対象の型を
   直接f-string補間する箇所は必ず影響を受けるため、新規にEnum化する
   際は最初から`__str__`をoverrideし、f-string出力を直接assertする
   回帰テストを1本必ず追加する。
2. **テキスト経由で値を再パースする下流消費者の洗い出しを優先する**:
   json.dump()経由（`GrowthVerdict`のverdict等）はEnum化の影響を受けない
   一方、`report_consistency_check.py`のNG-3のようにreport.txtの
   テキストをregexで再パースして比較するチェックは、`__str__`override
   漏れがあると全銘柄で誤発火する。Enum化の事前調査では「値がどこに
   JSON/dictとして渡るか」だけでなく「値がテキスト化されて後で
   再パースされる箇所がないか」を必ず洗い出す。

**実データ検証時の技法（同教訓）**: 実銘柄でpipeline.py等を再実行して
新旧比較する際、分類結果自体が変化するケースに遭遇したら、まず
「同一の入力データ（コミット済みlatest.json等をそのまま関数に再投入）で
新旧コードを比較する」フローズン入力比較を行い、市場データの日次変動
（株価・成長率の再取得による境界値またぎ）による正当な変化なのか、
実装変更由来の regression なのかを切り分ける。GATE2-PHASE3B-1③-bの
CON銘柄（TRIM→GROWTH_PREMIUM変化）はこの手法で「約1%の株価変動が
既存の閾値`req_g < ttm_g`を跨いだだけ」と特定でき、一時停止せず
そのまま報告する判断ができた。

## Claude Code依頼文の形式
依頼文はチャット画面上にコードブロック（``` ```）で表示し、
ユーザーがそのままコピー&ペーストできる形式にする。
複数ステップの作業でも1つのコードブロックにまとめて一括コピー可能にする。
このルールは例外なく厳守する。コードブロック内にコードブロックをネストしてはならない。
ファイル出力（create_file）は行わない。

**完了報告の必須項目（2026-07-20の教訓）**:
Claude Codeの完了報告には、コミットハッシュを必ず明記する
（本体実装・BACKLOG更新等、コミットを分離した場合はそれぞれの
ハッシュを個別に記載する）。報告漏れが散発したため、完了報告の
標準項目として明記する。

## 回答の長さ
「詳しく説明して」等の明示的な依頼がない限り、回答は最低限の分量にする。

## 設計判断の確認方法
複数の実装方針が考えられる場合、コンセプトに反する選択肢を提示して
コンセプトが揺らぐ原因を作ってはならない。
全ての選択肢がコンセプトに適合する場合は、推奨案とその理由をあわせて
提示すること。

提示文体の例：
「コンセプトはこうなので、こちらを推奨します。よろしいですか？」
（例: 「TANUKI SCOREはファンダ×タイミングで判断するのがコンセプトなので、
A案を推奨します。よろしいですか？」）

**適用範囲の注意（2026-06-21の教訓）**：
この確認方法は「複数の実装方針が選べる設計判断」のためのものであり、
不具合修正の対応範囲を絞る理由に使ってはならない。不具合修正は
原則として根本対応する（応急処置で済ませず、恒久対応・再発防止まで
含めて1件として扱う）。「対応①は今すぐ、対応②は別タスク」のように
切り分ける場合も、コンセプトを理由にせず、緊急性・作業の安全性等の
実務的な理由で説明すること。

## 認識の食い違いへの対応
ユーザーの前提認識（「〜という指示をしたはずだ」等）と、調査結果
（リポジトリ記録上の事実）が食い違う場合、チャット側Claudeは記録上の
事実を理由に結論を急がない。まず食い違いの所在を確認する質問を行い、
ユーザーの認識を正確に把握してから、その認識を前提とした選択肢を
再整理する。記録に基づく理由付けでユーザーの認識を上書きしようとしない。

## 提案条件の網羅性確認（2026-07-12の教訓）
修正案・実装依頼書を作成する前に、関連するデータの条件分岐（判定ロジック・
優先順位・フォールバック等）を網羅的に把握し、抜け漏れ・不整合がないか
確認する。Claude Codeからの報告文の記述のみを根拠に設計判断をしない。

（実例: SEC-TAG-FICO-CPRT-1で、報告文中の「KLAC型修正で解消済みの
可能性が高い」という一文のみを根拠に確認依頼を出したところ、実際は
無関係な別原因（`_extract_values_merged()`の早い者勝ちマージ）だった。
対応方針を決める前に、まず条件分岐の網羅調査を独立のステップとして
挟むこととする）

## 候補修正の全母集団シミュレーション（2026-07-19の教訓）

特定の問題銘柄（例: MOのfloor問題）への対処案を設計する際、その案が
影響しうる全母集団（同じ判定ロジック・同じ閾値を通過する全銘柄）に
対して事前にシミュレーションを行い、意図した対象以外への副作用（既に
正常に機能している銘柄を悪化させないか）を確認してから実装依頼を
作成する。問題銘柄だけを見て「効くかどうか」を判断し、影響範囲全体を
確認しないまま実装すると、対象外の銘柄に新たな回帰を生むリスクがある。

（実例: GROWTH-SANITY-CLASS-SYNC-1のMO floor問題対応で、当初提案した
「recommended_g候補条件を2件→1件に緩和」という案は、MO単体では
効果を検証したが、同じ緩和ルールが適用される37銘柄全体でシミュレー
ションしたところ、LOAR（真の高成長IPO銘柄）で成熟業種平均が誤って
成長率に採用され、実態の1/5に圧縮されるという新たな害があると判明。
実装前に全母集団確認を挟んだことで回避できた。同日のFCF調査でも、
ticker_overrides除外を検討する際に「事前調査で全24銘柄との重複を
機械確認する」ことで同種の誤爆〈GOOGL/MSFT〉を回避した実績があり、
この手法自体は既に実践していたが、明文化されていなかったため今回
標準ルールとして追加する）

**実務上の手順**: 対処案が「特定条件を満たす銘柄すべて」に適用される
設計の場合、以下を実装依頼の前に必ず確認する：
1. その条件に該当する全銘柄をリストアップする
2. 現状（対処前）の値と対処後の値を全銘柄で比較する
3. 意図した対象（問題銘柄）以外で値が変化する銘柄がないか確認する
4. 変化がある場合、その変化が改善か悪化かを個別に判定する

**追記（GROWTH-VERDICT-SEQUENCING-BUG-1、2026-07-19の教訓・2件）**:

① 変更が特定フィールドの再計算のみ（DCF計算式自体は不変）の場合、
全銘柄でpipeline.pyをフル再実行しなくても、既存latest.jsonのデータと
対象関数（例: `check_growth_sanity()`）を、Pipelineクラスの読み取り専用
ヘルパーメソッド（`_load_beta_sector()`等）経由で取得した入力値と
組み合わせてオフラインで直接呼び出すことで、同じ結果を高速かつ
安全にシミュレーションできる（61銘柄で実施し、実際のpipeline.py実行
結果と完全一致を確認済み）。全銘柄フル再実行が高コスト・低速な場合に
有効な軽量シミュレーション手法。

② 修正対象のフィールドが**複数の下流消費者**（例:
`growth_sanity.phase1_growth`はreport.txt/stock.html表示とTANUKI SCORE
のGROWTH_PREMIUM判定`pipeline.py:487`の両方から参照されていた）から
参照される共有オブジェクトの場合、「主たる表示・verdict」だけでなく、
**そのフィールドを参照している全ての下流消費者**について個別に
シミュレーション・影響確認を行う。主目的（verdict改善）の検証だけに
留めていた場合、TANUKI SCORE側の別の実害（CON: GROWTH_PREMIUM誤判定）を
見逃すところだった。grepでフィールド名の参照箇所を横断的に洗い出して
から影響確認の対象を確定すること。

## 探索的スキャンツールと常設WARN条件の分離（2026-07-19の教訓）

「同種の候補が他にもないか横断的に洗い出す」ための探索ツール（クラス
タリング・統計的閾値スキャン等）と、`report_consistency_check.py`の
ような常設ゲートに組み込むWARN発火条件は、**同じロジックをそのまま
流用しない**。探索ツールは「人間が結果を目視確認する前提」で多少の
過検知（false positive）を許容できるが、常設WARNは無条件に毎回発火
するため、過検知があると誤警告が蓄積し警告の信頼性を損なう。

（実例: [[FYE-CHANGE-BOUNDARY-COLLISION-BLIND-1]]で、決算期変更境界の
候補抽出用に`fye_change_candidate_scan.py`という独立のクラスタリング
スキャンツールを新設したが、これをそのままWARN-24の発火条件に転用
しようとしたところ、「同一決算アンカー・fyタグが1つずれているだけ」の
無害なケース〈ADSK/AVAV/CAKE/CRM等〉まで誤検知する過剰検知バグが
判明した。WARN-24側には別途アンカー日距離によるフィルタ
〈`_fiscal_anchors_far_apart()`〉を追加し、真に「決算期そのものが
変更された」ケースのみに絞り込むことで解消した）

**実務上の指針**: 探索ツールを常設チェックに転用する際は、探索ツール
側の閾値・アルゴリズムをそのまま持ち込まず、常設チェック用に独立して
「本当に発火させるべき条件」を精査・追加実装すること。探索ツール自体は
「補助的な候補抽出手段」としてリポジトリに残し、定期的な手動棚卸し用途
に限定する。

## 作業範囲の事前宣言
「記録のみ」「調査のみ」「実装まで」など、依頼の範囲が曖昧な場合は
着手前に一言で確認する。

## 調査の鉄則
表示不具合の調査は必ず「fetchパス→データ存在→ロジック」の順で行う。

調査依頼を作成する際は、ユーザーから直接聞かれていなくても、その調査結果
次第で当然次に聞かれるであろう関連確認（例: 同じデータが別経路で表示
されている箇所はないか、似た症状が他のシンボル・他の画面にもないか）を
洗い出し、最初の依頼文に含める。後出しで「これも確認して」を繰り返させない。

## 銘柄スクリーニング着手前の確認事項（2026-07-10の教訓）
複数銘柄の比較・スクリーニング・推薦作業に着手する前に、個別データの
取得・組み合わせに入る前に、まず以下の既存の統合アウトプットの有無を確認する：
- TANUKI SCORE（Classification: BUY/WATCH/HOLD/TRIM/GROWTH_PREMIUM/SELL/PASS）
- Extreme Fear TOP10（買い候補ランキング機能）
- 各銘柄のreport.txt（統合レポート・AI用）
これらの存在を確認しないまま個別JSONの断片から独自にスクリーニングロジックを
組み立てると、既存機能の車輪の再発明になり、かつ既存機能が持つチェック
（成長率警告等）を見落としたまま作業が進むリスクがある。

**標準フローの自動適用（2026-07-10の教訓）**：
「銘柄を見繕って」「スクリーニングして」等の簡潔な依頼を受けた場合、
以下の標準フローを、都度の確認や前提条件のヒアリングを挟まず一括で実行する：
1. TANUKI SCORE・Extreme Fear TOP10等の既存アウトプットを確認
2. common/screening/report_txt_parser.py で対象銘柄群のreport.txtを一括取得
3. common/screening/dcf_validity_checker.py で妥当性チェック
4. 上記2つの結果を突き合わせ、双方でクリーンな銘柄を絞り込んで提示
「短期決着したい」「特定テーマに絞りたい」等の追加条件は、初回の結果を
提示した後にユーザーから伝えられた時点で反映し、再スクリーニングする。
初回依頼時にこれらの条件出しを求める必要はない。

## 外部要因と結論づける前の確認（2026-06-21の教訓）
「データソース側の問題」「外部要因」と結論づける場合、自分たちの取得
コードが失敗しているだけで、データソース自体には実際にはデータが
存在するケースがあることに注意する。可能であれば、データソースの
一次情報（公式サイト等）で実際にデータの有無を確認してから結論づける。
取得処理が「成功・失敗」のどちらだったかと、データソースに「データが
存在する・しない」かは別軸であり、混同しない。

## 無関係コミットの巻き込み禁止
クリーンアップ・関連修正のついでに、無関係な既存コミットをrevertしない。

## memoryとの関係
本ファイルがチャット側Claudeの運用ルールの正本。
Claudeのmemory機能に同種のルールが残っている場合、本ファイルの内容を優先する。
本ファイルを更新した際は、memory側の古い記述と矛盾しないか確認する。

## 作業完了時の確認
作業完了をClaude Codeから報告された際、以下が実施されたか確認する：
- BACKLOG.md の該当項目削除・BACKLOG_DONE.md への移動
- SYSTEM_MAP.md の更新（新規ファイル追加・役割変更・依存関係変更時のみ）

**依存関係変更を伴う実装での自発的報告（2026-07-15の教訓）**：
構造的な依存関係の変更（あるモジュールが独自に持っていた処理を別
モジュールの既存機能へ統合する等）を伴う実装を完了した際、Claude Code
は明示的に「SYSTEM_MAP.mdを確認して」と聞かれる前に、完了報告へ
「SYSTEM_MAP.mdへの影響有無」を自発的に含めることが望ましい。実例
（reader.py/pipeline.pyのBS取得ロジック一本化）では、ユーザーから
明示的に確認を求められるまでこの言及がなかった。上記のチャット側
Claudeによる事後確認はあくまで見落とし防止の最終防波堤であり、
Claude Code自身が完了報告の標準項目として扱うべき。

**事前にBACKLOG化されていなかった不具合修正の扱い（2026-06-21の教訓）**：
ユーザー報告から発覚し、その場で調査・修正した不具合（事前にBACKLOG.mdに
項目化されていなかったもの）も、応急処置・恒久対応・根本修正等、実質的な
作業が発生した場合はBACKLOG_DONE.mdへの記録対象とする。「事前登録が
なかったから記録不要」と自動的に判断せず、作業内容の重み（再発防止の
教訓を含むか、複数段階の対応だったか等）を見て記録の要否を都度判断し、
判断に迷う場合はユーザーに確認する。

**着手条件付きタスクの記録更新（2026-06-24の教訓）**：
あるタスクの「着手条件」として別の作業が紐づいている場合、
その条件作業が完了した時点で、親タスクのBACKLOG.mdも
「条件満たされた」旨に更新する。
条件作業が独立したBACKLOGタスクでない場合でも同様。

未実施であれば、その時点でClaude Codeへの依頼文を作成する。

**「次セッション着手順序」欄の都度更新（2026-07-20の教訓）**:
BACKLOG.md末尾の「次セッション着手順序」欄の陳腐化は2026-07-08・
07-12・07-13・07-19・07-20と繰り返し発生している。原因は、この欄への
✅反映をセッション終了時の一括ブラッシュアップにのみ委ねていたこと。
今後は、個々のタスクが完了しBACKLOG_DONE.mdへ移動する都度、その
完了報告依頼に「次セッション着手順序欄の該当項目に✅を付け、次点を
繰り上げる」ことを明示的に含める。セッション終了時のブラッシュアップは、
この都度更新が漏れていないかの最終確認として位置づけ、都度更新の
代替にはしない。

**新規発見事象のBACKLOG.md登録徹底（2026-07-20の教訓）**:
調査・実装検証の過程でスコープ外の新規事象を発見した場合、Claude Code
は実害の有無・重要度の自己判断でBACKLOG.mdへの登録を省略しては
ならない。実害なし・優先度低と見積もられる場合でも、必ず独立した
BACKLOG.md新規タスクとして登録し（例: 優先度「低〜未定」で登録）、
重要度の最終判断はチャット側Claude・Koichiさんに委ねる。
BACKLOG_DONE.mdの完了記録内への言及のみでは登録として不十分。

## 調査・作業の継続性管理

### 完了報告の確認
Claude Codeから完了報告が届いた場合、その内容を確認してから次の指示を出す。
完了報告が届いていない状態で同一内容の依頼文を再送しない。
「完了しましたか？」と確認してから次に進む。

### 調査結果の重複防止
同一の調査依頼を複数回送信しないよう、直前のターンで得られた情報を
必ず確認してから次の調査依頼を作成する。
調査結果がすでにチャット上にある場合は、それを前提として次の判断を行う。

### 調査依頼の精度向上
調査依頼のパスやコマンドは、想定されるディレクトリ構造を事前に確認してから
指定する。パスが不明な場合は「find」等で先に確認する調査ステップを含める。
False Alarmを減らすため、パス・フィールド名・変数名は推測で指定しない。

### 一括調査の原則
関連する複数の確認事項は、個別に分けて後出しにせず、最初の調査依頼に
まとめて含める。「これも確認して」を繰り返させない（調査の鉄則と同じ原則）。

---

## 一日の作業終了時のブラッシュアップ
セッション最後の作業として以下を実施する：

### BACKLOG.mdの整合性確認
- 本日完了したタスクが「次セッション着手順序」に✅で記録されているか
- 完了日が正しいか（Claude Codeが誤記することがある）
- 新規発見した課題がBACKLOGに登録されているか
- 冗長な区切り線・空白行がないか

### BACKLOG_DONE.mdの記録確認
- 本日完了した全タスクのエントリが存在するか
- 事前にBACKLOG化されていなかった作業（その場で発生した修正等）も記録されているか

### 生成物（ゴミ）の除去確認（2026-07-10追加）
- 当日のセッションで生成した一時ファイル・使い捨てスクリプトが
  リポジトリのワーキングツリーに残っていないか `git status` で確認する
- 分析用の一時JSON等は .gitignore 済みか、または削除したか確認する
- 試作版スクリプト（正式格納前の中間版）の残骸がないか確認する

### CHAT_RULES.md・CLAUDE_CODE_START.md・SYSTEM_MAP.mdの確認
- 本日の作業を通じて判明したルール・手順の改善点があれば追記する
- 新規ファイル追加・役割変更・依存関係変更があればSYSTEM_MAP.mdを更新する

### SYSTEM_MAP.mdの更新確認（作業終了時にも実施・2026-07-10追加）
- 当日の作業で新しいデータ生成物・スクリプト・システム間依存・
  銘柄振り分け構造の変更が生じた場合、SYSTEM_MAP.mdに反映されているか確認する
  （※従来は「新規ファイル追加・役割変更・依存関係変更時のみ」としていたが、
  本日のように既存構造の未記載が後から判明するケースがあるため、
  作業終了時の点検項目としても明示する）

---

## 月次メンテナンス
月初の最初の作業時、CLAUDE_CODE_START.mdの「月次メンテナンスタスク」
（フロントエンド表示の陳腐化チェック・SYSTEM_MAP.md確認等）が
実施済みか確認する。未実施なら実施をKoichiさんに提案する。
