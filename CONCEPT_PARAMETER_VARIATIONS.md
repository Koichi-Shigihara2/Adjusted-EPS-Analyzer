# CONCEPT_PARAMETER_VARIATIONS.md — 概念統一・パラメータ違いの記録

作成日: 2026-07-22
更新日: 2026-07-23（軸の一般化。期間軸1本のみだった対象を6軸に拡張し、
`FIELD_DEFINITIONS.md`定義済みの499項目全件を対象に横断的に再点検した）
出発点: `OUTPUT_ITEMS_INVENTORY.md`（AS-IS全515項目）、`TO_BE.md`（16群統一定義）、
`TO_BE_FINAL_LIST.md`（同一定義／異なる定義の項目単位判定）、
`FIELD_DEFINITIONS.md`（499項目の計算式最小単位分解、2026-07-23完了）

## 本ドキュメントの位置づけ

`TO_BE_FINAL_LIST.md`で「異なる定義」に分類されていた項目のうち、実際には
**同一のビジネス概念・計算対象でありながら、特定のパラメータ軸のみが
異なる**ペア・グループが存在することが判明した。これらを第3カテゴリ
「概念統一・パラメータ違い」として本ドキュメントに記録する。

当初（2026-07-22作成時点）は「集計期間」という1軸のみを対象としていたが、
499項目確定後の横断的再点検で、期間以外にも同種のパラメータ違いが複数の
軸で存在することが判明したため、以下6軸に一般化した（軸1が当初からの
対象、軸2〜6が2026-07-23の再点検で追加）。

1. **軸1: 期間・集計期間**（TTM/FY/YoY/3年CAGR/月次/週次/日次等）
2. **軸2: 取得元・データソース**（同一対象を異なる外部API/データソースから取得）
3. **軸3: 算出基準・集計方法**（同一名称でも分母・集計方法が異なる）
4. **軸4: 通貨・単位**（同一概念を異なる通貨・単位で表現）
5. **軸5: シナリオ・前提条件**（同一の計算対象を異なる前提で複数算出）
6. **軸6: 集計範囲・対象粒度**（個別銘柄値 vs 業種平均値・全銘柄平均値等）

**重要**: このカテゴリは項目を削除・統合するものではない。各パラメータ
バリエーションは、それぞれ固有の目的（例: STONKS SILOのAnnual基準は
プレレベニュー企業のSEC実績重視、HypeCoreのTTM基準は市場の直近評価反映）
のために個別に存置される。本ドキュメントは「これらが同じ根本概念の異なる
窓（パラメータ軸上の異なる値）である」という関係性を記録し、将来の
`NAMING_CONVENTIONS.md`（命名規則）適用時の参照台帳とすることを目的とする。

実装（コード修正）は行っていない。分類の見直しと記録のみ。

## 判定基準（軸共通）

以下をすべて満たすペア・グループを「概念統一・パラメータ違い」と判定した:
1. 同一の根本的な財務指標・データ項目・業務概念を測定対象としている
   （例: 売上高、純利益、OCF、PSR＝時価総額÷売上高、S&P500指数値）
2. 実際にコードを確認し、同一の生データフィールド（同一XBRLタグ相当・
   同一yfinanceフィールド）または同一の`common/sec_data`系列・同一の
   計算目的を参照していることを確認した
3. 相違点が特定の1軸（上記6軸のいずれか）のみであり、測定対象の
   財務指標・業務概念そのものは変わらない

この基準を満たさない場合（測定対象の財務指標・概念そのものが異なる等）は
「異なる定義」のまま維持する。判定の過程で**棄却した候補**
（③成長率系のTANUKI FCF CAGR、⑬Rule of 40）も本文末尾に理由とともに
記録する。

**499項目全件を対象とした横断点検であることの確認**: 本ドキュメントが
対象とするAS-IS-IDは、いずれも`FIELD_DEFINITIONS.md`に現に定義済みの
499項目に含まれるものである。過去の調査（`TO_BE.md`⑫群、net_cash）で
発見された`AS-IS-134`（STONKS SILO `valuation.net_cash`）は、515→503件
統合の時点で既に`AS-IS-025`への統合対象として削除済み（`TO_BE_FINAL_LIST.md`
`DELETED`集合に含まれる）であり、現行499件には存在しないため、本ドキュメント
（499項目ベース）の対象からは除外する。

---

# 軸1: 期間・集計期間（2026-07-22作成、内容変更なし）

## 概念1: PSR（株価売上高倍率）

**共通の計算対象**: 時価総額（または株価）を売上高で除した倍率。「割高/割安」
の簡易指標として3サブシステムが独立に保持。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | 期間パラメータ | 値の説明 |
|---|---|---|---|
| AS-IS-032（内包、`ps`サブフィールド） | TANUKI VALUATION | TTM | `data_fetcher.py:532` `info.get("priceToSalesTrailing12Months")`。**注記**: AS-IS-032はper/peg/ev_ebitda等を含む束ねられた行のため、`ps`はその一部のサブフィールドとして概念1に属する（AS-IS-032自体は他の非PSRフィールドを含むため「異なる定義」プールにも残る） |
| AS-IS-099 | HypeCore | TTM | `hypecore.py:118` `info.get("priceToSalesTrailing12Months")`。TANUKIの`ps`と**全く同一のyfinanceフィールド**を参照（取得タイミングのみ非同期） |
| AS-IS-132 | STONKS SILO | Annual | `pipeline.py:127` `market_cap / latest_rev`（`latest_rev`は`common/sec_data`の`revenue_sanitized`、直近通期決算）。TTM基準の2件とは分母の期間が異なる |

**実データ突合（本日別調査で既実施）**: AVAV等の黒字銘柄でTTM同士（032/099相当）はほぼ完全一致を確認済み。Annual基準（132）はIONQ等で最大1.75倍の乖離を確認済み（`TO_BE.md`⑤群参照）。

---

## 概念2: net_income（純利益）

**共通の計算対象**: SEC XBRL `NetIncomeLoss`相当の純利益額。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | 期間パラメータ | 値の説明 |
|---|---|---|---|
| AS-IS-129 | STONKS SILO | FY（単年度決算） | `pipeline.py:111-117`、`common/sec_data`の年次正規化パイプライン経由 |
| AS-IS-281 | EPS Analyzer | TTM（直近4四半期合算） | `pipeline.py:calculate_ttm`(288-304)、EPS Analyzer自身の独立XBRL抽出パイプライン経由 |

**実データ突合（本日別調査で既実施）**: 期間が実質整合する銘柄（AVAV, ESTC）では完全一致を確認済み。IONQ/IOT/ONDS等では期間差により符号反転を含む乖離を確認済み（`TO_BE.md`⑭群参照）。

---

## 概念3: 売上高成長率（Revenue Growth Rate）

**共通の計算対象**: 売上高の期間比較による成長率（%）。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | 期間パラメータ | 値の説明 |
|---|---|---|---|
| AS-IS-152 | STONKS SILO | 単年度YoY系列 | `analyzer.py:196-212` `(curr/prev - 1) * 100`（年ごと）、`common/sec_data`年次`revenue_sanitized`由来 |
| AS-IS-136 | STONKS SILO | 3年CAGR | `analyzer.py:215-222` `((r_end/r_start)**(1/3) - 1) * 100`（4年前と直近年を比較）、152と**同一の`revenue_sanitized`系列**を参照、窓の長さのみ異なる |
| AS-IS-093 | HypeCore | TTM YoY | `hypecore.py:162-164` `rev_ttm = rev.rolling(4).sum()`、`(rev_ttm / rev_ttm.shift(4) - 1) * 100`。**データソースに注意**: `{ticker}_quarterly_normalized.json`（SEC四半期正規化データ）由来であり、152/136の年次`annual_*.json`（`revenue_sanitized`）とは**別の正規化パイプラインを経由**している。同じ「SEC由来の売上高」という大分類には属するが、集計元ファイル自体が異なるため、152/136ほど厳密に「同一データソース」とは言い切れない点を明記する |

**新規発見**: AS-IS-136とAS-IS-152は`analyzer.py`内の同一関数・同一変数`revenues[yr]`から算出されており、**データソースの同一性が最も厳密に確認できるペア**。AS-IS-093は概念としては同じだが、正規化パイプラインの違いというprovenance上の注意点がある。

---

## 概念4: STONKS SILO OCF年次値（単年 vs 多年）

**共通の計算対象**: 年次営業キャッシュフロー（`cf.operating_cash_flow`）。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | 期間パラメータ | 値の説明 |
|---|---|---|---|
| AS-IS-156 | STONKS SILO（`_analyze_runway`） | 最新1年のみ | `analyzer.py:494` `ocf = cf.get("operating_cash_flow")`（`records[years[-1]]`、月次バーン計算用） |
| AS-IS-160 | STONKS SILO（`_analyze_profitability_path`） | 全年度（複数年dict） | `analyzer.py:551-553` `ocf_annual[yr] = records[yr]["cf"].get("operating_cash_flow")`（黒字化ロードマップのチャート用） |

**注記**: 156は160が保持する複数年dictのうち「最新1年分」のみを別の関数で再抽出したものであり、同一サブシステム内での軽微な重複実装。データソース・フィールドは完全に同一（`cf.get("operating_cash_flow")`）で、窓の長さ（1年 vs 全年）のみが異なる、最も明確な「概念統一・パラメータ違い」の例。

---

# 軸2: 取得元・データソース（2026-07-23新規追加）

## 概念5: S&P500指数終値

**共通の計算対象**: S&P500指数の終値。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | データソース | 値の説明 |
|---|---|---|---|
| AS-IS-190 | MACRO PULSE | FRED | `05_main.py:get_sp500()`825-830。FRED `SP500`系列を優先取得し、失敗時のみstooq（`%5Espx`）へフォールバック |
| AS-IS-312（内包、10指標束ね行のS&P500サブフィールド） | Market Pulse | yfinance | `collect_and_send.py`67-73等の`history()`。AS-IS-312自体は米10年債/VIX/ドル円/日経平均/NASDAQ/WTI/金/HYG/LQD等の非S&P500フィールドも含む束ね行のため、S&P500部分のみが本概念に属する（PSR概念1のAS-IS-032と同型の「内包」扱い） |

**既存発見の統合**: 本ペアは`FIELD_DEFINITIONS.md`フェーズ1（AS-IS-190）・
フェーズ10（AS-IS-312）双方の備考で既に「複数取得経路あり」として個別に
指摘済みだったものを、本ドキュメントの軸2として統合整理した。

---

## 概念6: 信用市場ストレスの代理指標（クレジット環境判定）

**共通の計算対象**: 信用市場のストレス度合い（クレジットスプレッド拡大＝
リスクオフの兆候）を判定するための入力データ。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | データソース | 値の説明 |
|---|---|---|---|
| AS-IS-194 | MACRO PULSE | FRED（実測スプレッド） | `BAMLH0A0HYM2`（ハイイールド債OASスプレッド）の実際の数値。RECESSION RISK SCORE（AS-IS-215）の8指標中15%ウェイトを占める直接入力 |
| AS-IS-368 | Market Pulse | yfinance（ETF価格比の代理指標） | `credit.credit`＝`(HYG.change_percent - LQD.change_percent) < 0`という、ハイイールド債ETF(HYG)と投資適格債ETF(LQD)の**当日価格変化率の差**による代理判定。実際のスプレッド水準ではなく日次の相対値動きのみを見る |

**既存発見の統合**: `TO_BE.md`⑮群の「関連する確認済み事項」で
「Credit判定: Market PulseはHYG/LQD ETF価格比の代理指標(日次リスクオン/オフ)、
MACRO PULSEは実際のOASスプレッド値(景気後退確率への15%ウェイト寄与)。
計算根拠が別物」として統合不要と判断済みだった事項を、軸2（データソースの
系統差: FRED公式マクロ系列 vs yfinance ETF価格）の観点から本ドキュメントに
初めて整理した。**注記**: MACRO PULSE内部の`AS-IS-194`/`AS-IS-199`は同一
FRED系列の内部重複取得（`TO_BE.md`⑮群、統一候補）であり軸2の対象外。
軸2の対象はあくまで「MACRO PULSE系(FRED)」と「Market Pulse系(yfinance)」
という2つの独立した情報源の間の差である。

---

# 軸3: 算出基準・集計方法（2026-07-23新規追加）

## 概念7（軸1・3の複合、概念1のクロス参照）: PSRのAnnual基準 vs TTM基準

**共通の計算対象**: 概念1（PSR）と同一（AS-IS-032/099/132）。

**軸3の観点からの整理**: 概念1はTTM(AS-IS-032/099)とAnnual(AS-IS-132)の
差を「期間パラメータの違い」（軸1）として記録済みだが、同時に
**分母（売上高）の集計基準そのものが異なる**（TTM＝直近4四半期合算 vs
Annual＝直近通期決算という「集計方法」の違い）という軸3の性質も併せ持つ
複合事例である。新規のAS-IS-IDは追加しない（概念1と同一項目を指すため、
本表では件数の独立カウントは行わない）。詳細・実データ突合結果は
軸1・概念1を参照。

---

## 概念8: 利益率（Profit Margin）のサブターム

**共通の計算対象**: 収益性を測る「利益率」という概念そのもの
（Rule of 40の利益率項として使われる）。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | 算出基準 | 値の説明 |
|---|---|---|---|
| AS-IS-095（内包、`rule40`内の`op_margin`項） | HypeCore | 純利益率（NI基準） | `hypecore.py` `op_margin = NetIncome/Revenue×100`（単一四半期の値同士の比率） |
| AS-IS-143（内包、`rule_of_40`内の利益率項） | STONKS SILO | 営業利益率（OperatingIncome基準） | `analyzer.py` `operating_income/revenue_sanitized×100`（annual値） |

**既存発見との関係**: 本文末尾「精査した結果、棄却した候補」の⑬Rule of 40
（2026-07-22時点の判定）は、Rule of 40という**合成指標全体**を軸1（期間）
の観点から精査し、利益率項の会計上の定義差（純利益率 vs 営業利益率）を
理由に「異なる定義のまま維持」と判定していた。この判定自体は変更しない
（合成指標としては引き続き統合しない）が、**利益率サブターム単体**を
軸3（算出基準・集計方法：NI基準 vs Operating Income基準という分子の違い）
の観点から見ると、「同じ『利益率』という概念の、分子（会計上の利益の
定義）が異なるパラメータバリエーション」として本ドキュメントの対象に
新たに含めることが妥当と判断した。AS-IS-095/143自体は「異なる定義」の
まま維持し、削除・統合は行わない。

---

# 軸4: 通貨・単位（2026-07-23新規追加）

## 概念9: Portfolio資産評価額（USD/JPY）

**共通の計算対象**: 保有資産の合計評価額。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | 通貨単位 | 値の説明 |
|---|---|---|---|
| AS-IS-391 | Portfolio | USD | `total_assets_usd = total_equity_usd + total_cash_usd`（`portfolio.json`の保有株数×価格＋現金、全ブローカー合算） |
| AS-IS-392 | Portfolio | JPY | `total_assets_jpy = total_assets_usd × usdjpy`（`usdjpy`＝AS-IS-390、Market Pulseのドル円レートAS-IS-312を再利用した既定義の移送データ） |

**注記**: AS-IS-392はAS-IS-391の単純な為替換算（掛け算1回）であり、
分母・集計方法自体に相違はない（軸3ではなく純粋な軸4の事例）。
なお`total_pnl_usd`（AS-IS-393）にはJPY建ての対応項目は存在せず、
USD単独出力に留まっている。

---

# 軸5: シナリオ・前提条件（2026-07-23新規追加）

## 概念10: Bear/Base/Bullシナリオ評価

**共通の計算対象**: 悲観・標準・楽観という3つの前提シナリオ別の企業価値・
成長率予測。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | シナリオ算出方式 | 値の説明 |
|---|---|---|---|
| AS-IS-015 | TANUKI VALUATION | 決定論的乗数 | `calculate_scenario_valuations()`。`bear_rate=base_growth_rate×0.7`／`base_rate=base_growth_rate`／`bull_rate=base_growth_rate×1.2`という固定倍率をAS-IS-012（高成長率）に適用し、シナリオ別DCFを再計算 |
| AS-IS-027 | TANUKI VALUATION | 決定論的（AS-IS-015のGを流用） | `calculate_rice()`のRICE bear/base/bullは、AS-IS-015が算出した各シナリオのgrowth_rateをそのままRICE計算式の`G`項に代入 |
| AS-IS-064 | TANUKI VALUATION（stock.html） | 決定論的（クライアント側再計算） | `projectFuture()`。バックエンドのAS-IS-010を使わず、AS-IS-015のシナリオ別`rate`を使ってクライアント側で将来価値を独自に再計算 |
| AS-IS-492 | TANUKI TAIL | AI生成（Grok） | `build_stage2_prompt()`。決定論的な計算式は存在せず、直近KPI・Stage1評価結果・営業利益率推移をプロンプトに含め、Grokにbear/base/bull各シナリオの`revenue_growth_y1/y2/y3`をJSON生成させる |
| AS-IS-496 | TANUKI TAIL | AI生成（AS-IS-492と同一応答） | `stage2.scenarios.{...}.kpi_forecasts`。同一のGrok応答内でKPI予測値も生成させる（コード側での検算・固定式適用なし） |

**新規発見（最重要）**: 「Bear/Base/Bullシナリオ」という同一の業務概念が、
TANUKI VALUATION側では**決定論的な固定倍率（0.7/1.0/1.2）**による機械的
計算である一方、TANUKI TAIL側では**AI（Grok）による自由生成**という
全く異なるメカニズムで算出されている。前者は入力（base_growth_rateと
倍率）が同じであれば常に同じ結果を返す再現可能な計算だが、後者は
生成の都度、文言・数値が変動しうる非決定論的な出力である。同じ
「Bear/Base/Bull」というラベルを持つ値の性質（決定論的 vs AI生成）が
サブシステムによって根本的に異なる点は、利用者に明示されるべき情報である。

**関連する非数値フィールド（独立カウント対象外）**: AS-IS-495
（`stage2.scenarios.{...}.rationale`、シナリオ根拠の自由記述文）・
AS-IS-497（`stage2.key_assumptions`、全シナリオ共通前提の箇条書き）・
AS-IS-509（`"scenario": sc_name`、bear/base/bullのラベルをそのまま
格納するループ変数）は、いずれもAS-IS-492と同一のシナリオ機構に付随する
非数値・メタ情報フィールドであり、概念1のAS-IS-032と同様の理由で本表の
独立カウント対象には含めない。

**軸6との関連**: AS-IS-027（RICE）は本軸（シナリオ）に加え、下記
軸6（集計範囲）の概念12にも同時に該当する（RICEには「個別銘柄の
bear/base/bull」と「全銘柄平均」という2つの独立したパラメータ軸が
存在する）。

---

# 軸6: 集計範囲・対象粒度（2026-07-23新規追加）

## 概念11: Moat Score（個別銘柄 vs 全銘柄平均）

**共通の計算対象**: Moat Score（経済的濠スコア）。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | 集計範囲 | 値の説明 |
|---|---|---|---|
| AS-IS-026 | TANUKI VALUATION | 個別銘柄 | `calculate_moat_score()`が算出する個別銘柄のスコア（軸1〜5とは独立の既定義項目） |
| AS-IS-073 | TANUKI VALUATION（index.html集計表示） | 全銘柄平均 | `avg-moat = mean(全銘柄のcomponents.moat_score)`（`loadTickers():567-569`、nullは除外） |

---

## 概念12: RICE（個別銘柄 vs 全銘柄平均）

**共通の計算対象**: RICE（投資効率指標）。

**パラメータバリエーション**:

| AS-IS ID | サブシステム | 集計範囲 | 値の説明 |
|---|---|---|---|
| AS-IS-027 | TANUKI VALUATION | 個別銘柄 | `calculate_rice()`が算出する個別銘柄のRICE値（軸5の概念10にも該当、上記参照） |
| AS-IS-074 | TANUKI VALUATION（index.html集計表示） | 全銘柄平均 | `平均RICE = mean(全銘柄のrice.base.rice)`（AS-IS-073と同一パターンの別指標版、nullは除外） |

**注記**: AS-IS-072（銘柄数、`valid.length`の単純件数集計）は特定の
指標の「個別値」に対応する母体を持たない単純カウントであるため、
軸6の対象（同一指標の個別値とその集計値のペア）には含めない。

---

## 精査した結果、棄却した候補（第3カテゴリに該当しないと判定）

### ③成長率系: TANUKI「FCF CAGR(3yr)」(AS-IS-068) vs STONKS SILO「cagr_3yr」(AS-IS-136)
**棄却理由**: 名称は類似（共に"3yr CAGR"）だが、測定対象の財務指標そのものが異なる。
AS-IS-068はFCF（フリーキャッシュフロー）の3年複利成長率（stock.html:2106-2113
`(最新FCF/3年前FCF)^(1/3)-1`）、AS-IS-136は売上高の3年複利成長率
（`analyzer.py:215-222`）。「3年窓」という期間パラメータは共通するが、
FCF≠売上高であり測定対象の財務指標自体が異なるため、判定基準2（同一の
生データフィールドを参照）を満たさない。「異なる定義」のまま維持する。

### ⑬Rule of 40系: HypeCore「rule40」(AS-IS-095) vs STONKS SILO「rule_of_40」(AS-IS-143)
**精査結果**: 部分的に概念3（売上高成長率）を内包していることを新たに発見した。
- HypeCoreの`rule40 = rev_yoy + op_margin`の成長率項`rev_yoy`は、**AS-IS-093と
  完全に同一の値**（同じ変数を再利用、`hypecore.py:166-167`）
- STONKS SILOの`rule_of_40 = cagr_3yr + operating_income/revenue*100`の
  成長率項`cagr_3yr`は、**AS-IS-136と完全に同一の値**（同じ変数を再利用、
  `analyzer.py:245-249`）

しかし、Rule of 40全体としては**棄却**する。理由: 各々の「利益率」項
（HypeCore: 純利益率＝NI/Revenue、STONKS SILO: 営業利益率＝
OperatingIncome/Revenue）が、期間パラメータの違いではなく**会計上の
定義そのものが異なる指標**（純利益率と営業利益率は非営業項目・税金・
利息の扱いが本質的に異なる）であるため、Rule of 40という合成指標
全体を1つの「概念＋パラメータ違い」として扱うことはできない。
AS-IS-095／AS-IS-143は「異なる定義」のまま維持し、それぞれの成長率
サブコンポーネントが概念3と関連している旨のみ、概念3のテーブルに
注記として残す（既に上記に記載済み）。

### ⑦FCF系: TANUKI「fcf_base」(AS-IS-019) vs STONKS SILO「ocf_annual」(AS-IS-156/160)
**棄却理由**: `common/sec_data`の同一`cf`辞書を参照する点は`TO_BE.md`⑦群で
既に確認済みだが、TANUKIの`fcf_base`は`cf.get("free_cash_flow")`
（OCF−CapEx、FCF）を参照するのに対し、STONKS SILOの`ocf_annual`は
`cf.get("operating_cash_flow")`（CapEx控除前のOCF）を参照しており、
**フィールド自体が異なる**（FCF≠OCF）。判定基準2を満たさないため
「異なる定義」のまま維持する。

---

## 再集計への反映

### 軸別集計（2026-07-23更新、499項目ベース）

| 軸 | 概念数 | 独立カウント対象AS-IS-ID | 件数 |
|---|---|---|---|
| 軸1: 期間・集計期間 | 4概念（既存、変更なし） | AS-IS-099, 132, 129, 281, 152, 136, 093, 156, 160 | 9件 |
| 軸2: 取得元・データソース | 2概念（新規） | AS-IS-190, 194, 368 | 3件 |
| 軸3: 算出基準・集計方法 | 2概念（新規、うち概念7は概念1のクロス参照のため新規カウント0） | AS-IS-095, 143 | 2件 |
| 軸4: 通貨・単位 | 1概念（新規） | AS-IS-391, 392 | 2件 |
| 軸5: シナリオ・前提条件 | 1概念（新規） | AS-IS-015, 027, 064, 492, 496 | 5件 |
| 軸6: 集計範囲・対象粒度 | 2概念（新規） | AS-IS-026, 073, 027, 074 | 4件 |
| **軸別延べ合計** | **12概念** | （AS-IS-027は軸5・軸6の両方に該当するため2回計上） | **25件** |
| **ユニークAS-IS-ID数** | — | 軸1(9件)と軸2〜6の新規分(15件)は重複なし。軸2〜6内の重複はAS-IS-027の1件のみ（延べ16件→ユニーク15件） | **24件** |

**軸2〜6（新規追加分）の独立カウント対象15件の内訳**:
`AS-IS-190, 194, 368, 095, 143, 391, 392, 015, 027, 064, 492, 496, 026, 073, 074`
（15項目。AS-IS-027は軸5・軸6の双方に出現するが、このリストではユニーク
IDとして1回のみ計上している。延べ計上〈軸別集計表の「件数」列合計〉では
軸5・軸6でそれぞれ1回ずつ、計2回カウントしている点が上表との差分の由来）。

これらは削除・統合されるわけではなく、引き続き499件それぞれが個別の
出力項目として存在し続ける。変わるのは分類上の扱い（「異なる定義」
→「概念統一・パラメータ違い」という第3カテゴリへの移動、および同一
項目が複数の軸に同時該当しうるという事実の明示）のみであり、
**最終的な出力項目の実数（499件）に変化はない**。詳細は`TO_BE_FINAL_LIST.md`
のステップ5・`FIELD_DEFINITIONS.md`各フェーズを参照。

### 除外した既存発見（499項目の範囲外と確認したもの）

`TO_BE.md`⑫群で発見された`net_cash`の重複（AS-IS-025 vs AS-IS-134）は、
軸2（データソース: SEC XBRL vs yfinance）・軸3（算出基準: LT+ST有利子負債
控除 vs yfinance totalDebt控除）の両方に該当しうる事例だったが、
`AS-IS-134`は515→503件統合の時点で既に`AS-IS-025`へ統合され`DELETED`
（`TO_BE_FINAL_LIST.md`）として現行499件のカタログから除外済みである。
本ドキュメントは`FIELD_DEFINITIONS.md`に現に定義済みの499項目を対象と
するため、この事例は対象外とし、新規の軸2/3概念としては追加しない
（既存の`TO_BE.md`⑫群・`NAMING_CONVENTIONS.md`パターンAの記録で十分と
判断）。
